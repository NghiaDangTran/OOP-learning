r (let i in data) {
    //     array.push(new Huffman_Trees(i, data[i]));
    //     // array[i].printInorder();
    // }
    // //sort the array by smallest to largest
    // function compare(a, b) {
    //     return a.compareTo(b);
    // }

    // array.sort(compare);
    // for (let i = 0; i < array.length; i++) {
    //     array[i].printInorder(array[i], 0);
    // }

    // while (array.length >1) {


    //     // chose the first two elements
    //     // combine them into a new node
    //     // add the new node to the array
    //     //sort the array by smallest to largest
    //     array[0] = array[0].Combine(array[0], array[1]);
    //     // remove array[1]
    //     array.splice(1, 1);

    //     array.sort(compare);
    //     for (let i = 0; i < array.length; i++) {
    //         array[i].printInorder(array[i],0);
    //     }
    //     console.log("\n__________________ draw a line \n");

    // }

    // for (let i = 1; i < 8; i++) {
    //     console.log();