#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_INPUT 80
#define MAX_Length 100

int main()
{
  FILE *input1, *input2;
  char filename[MAX_INPUT];
  // *** declare more variables as required
char * line = NULL;
    size_t len = 0;
    ssize_t read=0;

   char a[2][14];
strcpy(a[0], "AB");
strcpy(a[1], "MB");



        printf("%s",a[0]);

    
  
  return 0;
}